class Monitor {
    constructor(id, resoluton, size, monitorType) {
        this.id = id;
        this.resolution = resolution;
        this.size = size;
        this.monitorType = monitorType;
    }
}

module.exports = Monitor;