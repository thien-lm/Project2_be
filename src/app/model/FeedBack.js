class FeedBack {
    constructor(id, userName, feedbackContent, rating, productID) {
        this.id = id;
        this.userName = userName;
        this.feedbackContent = feedbackContent;
        this.rating = rating;
        this.productID = productID;
    }
}

module.export = FeedBack;